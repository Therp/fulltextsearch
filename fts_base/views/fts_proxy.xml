<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="fts_proxy_search" model="ir.ui.view">
        <field name="name">fts.proxy.search</field>
        <field name="model">fts.proxy</field>
        <field name="arch" type="xml">
            <search string="Fulltext search">
                <field name="searchstring" />
                <field name="res_name" />
                <field name="res_model" />
                <filter string="Date Created" name="create_date_filter" date="date" />
            </search>
        </field>
    </record>

    <record id="fts_proxy_tree" model="ir.ui.view">
        <field name="name">fts.proxy.tree</field>
        <field name="model">fts.proxy</field>
        <field name="arch" type="xml">
            <list
                string="Fulltext search - results"
                create="false"
                delete="false"
                edit="false"
            >
                <button
                    name="action_open_document"
                    type="object"
                    icon="fa-pencil"
                    title="Open document"
                />
                <field name="res_name" />
                <field name="date" />
                <field name="extra" />
                <field name="summary" invisible="not context.get('fts_summary')" />
                <field name="rank" />
                <field name="res_model" />
            </list>
        </field>
    </record>

    <record id="action_fts_proxy_search" model="ir.actions.act_window">
        <field name="name">Fulltext search</field>
        <field name="res_model">fts.proxy</field>
        <field name="view_mode">list</field>
        <field name="context">{"fts_summary": False}</field>
        <field name="view_id" ref="fts_proxy_tree" />
        <field name="search_view_id" ref="fts_proxy_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Choose some filters and then some text for Full Text search.
                No search will be done, before a text is entered.
            </p><p>
                You can select one or more of the configured data collections
                (models), like emails or attachments. If you do not select any,
                all data collections will be searched.
            </p><p>
                For each model a maximum of 1024 entries will be found (80 when
                searching with summary, as generating highlights is computationally
                expensive). If those do not contain what you are looking for, it is
                either not there, or you can add additional search criteria.
            </p><p>
                You can specify criteria this way:
                <ul style="background-color: white; text-align: left;">
                    <li>A word - will find all entries containing that word.</li>
                    <li>Some text follwed by a "*" - will find all entries that have
                        a word starting with that text.
                    </li>
                    <li>Entering multiple words will find entries containing any
                        of those words. So entering 'cat' and 'dog' will find all
                        entries that have the word cat, or the word dog, or both.
                    </li>
                    <li>Entering multiple words between double quotation marks,
                        like "cats and dogs" will find only the entries that
                        contain that exact frase.
                    </li>
                    <li>Entering multiple words, each followed by SHIFT+ENTER will
                        find those documents that contain all of the words.
                    </li>
                </ul>
            </p><p>
                Note: to prevent unneeded searches, first enter all other criteria, like
                models to search, or date, and only then the words to search for.
            </p>
        </field>
    </record>

    <record id="action_fts_proxy_search_with_summary" model="ir.actions.act_window">
        <field name="name">With summary</field>
        <field name="res_model">fts.proxy</field>
        <field name="view_mode">list</field>
        <field name="context">{"fts_summary": True}</field>
        <field name="view_id" ref="fts_proxy_tree" />
        <field name="search_view_id" ref="fts_proxy_search" />
    </record>

    <menuitem
        id="menu_fts_proxy_search"
        action="action_fts_proxy_search"
        web_icon="images/fulltextsearch.png"
    />

    <menuitem
        id="menu_fts_proxy_search_with_summary"
        action="action_fts_proxy_search_with_summary"
        parent="menu_fts_proxy_search"
        sequence="32"
    />

</odoo>
