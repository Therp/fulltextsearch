# Fulltext search for documents

Note that only the field index_content is searched, so to get most out of this module,
you should also install the knowledge module which provides automatic indexing of
attachments.
